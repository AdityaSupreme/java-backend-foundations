## Key TakeAways
- static frames are isolated per method
- Instance variables live on heap as part of object
- Method access instance state via `this`
- Static methods have no `this`, so no instance access
